# Cashlit does not use environment variables at runtime.
# API keys are entered through the UI and stored locally by the app.
#
# This file exists only as a reference for any future configuration needs.
# Copy to .env.local and fill in values if needed.

# Example (not used currently):
# VITE_APP_ENV=development
